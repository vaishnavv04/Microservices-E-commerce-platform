# Database Configuration (used by Docker Compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ============================================
# SUPABASE CONFIGURATION (Single Database)
# ============================================
# For Supabase, use a SINGLE database with all tables
# Get your connection string from: Supabase Dashboard > Settings > Database > Connection string
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
#
# Option 1: Single DATABASE_URL for all services (recommended for Supabase)
# Use Session Pooler for IPv4 compatibility (recommended)
# Format: postgresql://postgres:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
# Or Direct Connection (IPv6 only): postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:your_password@db.your_project_ref.supabase.co:5432/postgres
#
# Option 2: Service-specific URLs (if you want to use different databases)
# DATABASE_URL_USERS=postgresql://postgres:postgres@localhost:5432/ecommerce_users
# DATABASE_URL_PRODUCTS=postgresql://postgres:postgres@localhost:5433/ecommerce_products
# DATABASE_URL_ORDERS=postgresql://postgres:postgres@localhost:5435/ecommerce_orders
#
# Note: Cart service now uses Redis (see REDIS_URL below)
# ============================================

# Redis Configuration (for Cart Service)
REDIS_URL=redis://localhost:6379

# Service Ports (optional, defaults are set in docker-compose.yml)
USER_SERVICE_PORT=3001
PRODUCT_SERVICE_PORT=3002
CART_SERVICE_PORT=3003
ORDER_SERVICE_PORT=3004
PAYMENT_SERVICE_PORT=3005
NOTIFICATION_SERVICE_PORT=3006

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Stripe Configuration (OPTIONAL - can use mock mode)
# Get your keys from: https://dashboard.stripe.com/apikeys
# Leave as-is for mock/development mode - payment service will work in mock mode
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# SendGrid Configuration (OPTIONAL - can use mock mode)
# Get your API key from: https://app.sendgrid.com/settings/api_keys
# Leave as-is for mock/development mode - emails will be logged to console
SENDGRID_API_KEY=your_sendgrid_api_key
SENDGRID_FROM_EMAIL=noreply@ecommerce.com

# Twilio Configuration (OPTIONAL - can use mock mode)
# Get your credentials from: https://console.twilio.com/
# Leave as-is for mock/development mode - SMS will be logged to console
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number

